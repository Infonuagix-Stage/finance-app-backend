# railway.yaml (à la racine du repo)
services:
  - name: spring-boot-backend
    dockerfilePath: ./backend/Dockerfile
    # Si vous avez besoin de variables d'environnement dans le conteneur :
    env:
      PORT: 8080

